---
/*
 * Honeycomb — retired (no-op). The site-wide hexagon background in global.css
 * replaced these per-section watermarks. Renders nothing; kept so existing
 * <Honeycomb /> usages compile without changes.
 */
type Props = { opacity?: number; unique?: string };
---
